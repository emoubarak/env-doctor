DATABASE_URL:
  required: true
  format: url
PORT:
  required: false
  default: "3000"
  format: number
  min: 1
  max: 65535
API_KEY:
  required: true
  format: string
  min_length: 10
DEBUG:
  required: false
  format: boolean